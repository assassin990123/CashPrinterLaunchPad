{
  "name": "CashPad",
  "version": "1.0.0",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "hardhat test",
    "compile": "npx hardhat compile",
    "deploy-dogechain": "npx hardhat run scripts/deploy.js --network dogechain",
    "deploy-testnet": "npx hardhat run scripts/deploy.js --network dogechain-testnet",
    "deploy-bscMain": "yarn hardhat run scripts/deploy.js --network bscMain",
    "deploy-bscTest": "yarn hardhat run scripts/deploy.js --network bscTest"
  },
  "keywords": [
    "hardhat",
    "ethereum",
    "hackathon"
  ],
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.0",
    "@nomiclabs/hardhat-waffle": "^2.0.0",
    "@openzeppelin/contracts": "^4.7.2",
    "chai": "^4.2.0",
    "dotenv": "^16.0.1",
    "ethereum-waffle": "^3.1.1",
    "ethers": "^5.0.0",
    "hardhat": "^2.6.6",
    "husky": "^4.2.5"
  },
  "dependencies": {
    "eth-revert-reason": "^1.0.3"
  }
}
